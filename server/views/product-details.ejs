<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.15/dist/tailwind.min.css" rel="stylesheet">
    <title>Product Details</title>
</head>

<body>
    <div class="container mx-auto py-8">
        <h1 class="text-3xl font-bold mb-4">Product Details</h1>

        <div class="bg-white shadow-lg rounded-lg p-4">
            <h2 class="text-xl font-bold mb-2">
                <%= product.name %>
            </h2>
            <p class="text-gray-600 mb-4">
                <%= product.brand %>
            </p>
            <p class="text-gray-800"><strong>Category:</strong>
                <%= product.category.join(', ') %></p>
      <p class="text-gray-800"><strong>Sizes:</strong> <%= product.sizes.join(' , ') %></p>
      <p class="text-gray-800"><strong>Colors:</strong> <%= product.colors.join(' , ') %></p>
      <div class="flex justify-end mt-4">
        <a href="/admin/products/<%= product._id %>/edit" class="text-blue-500">Edit</a>
      </div>
    </div>
  </div>
</body>

</html>